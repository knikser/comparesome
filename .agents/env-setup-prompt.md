# Cloud Agent Environment Setup Prompt

Use this prompt with an env setup agent:

"Scan this repo and update the cloud agent environment so it preinstalls Go 1.23 toolchain, Node 22 + pnpm 10, and caches pnpm/go modules for this monorepo (frontend/*, backend/*)."
